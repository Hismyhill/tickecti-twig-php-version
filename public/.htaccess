<IfModule mod_rewrite.c>
  RewriteEngine On Options -Indexes DirectoryIndex index.php # Send all
  non-existing requests to src/index.php RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d RewriteRule ^ ../src/index.php [L]
</IfModule>
